# Nixpacks configuration for Coolify deployment
[build]
# Build commands for the project - uses production build by default
cmd = ["pnpm", "run", "build"]

[start]
# Serve the frontend and proxy API requests
cmd = "node ./server.js"



